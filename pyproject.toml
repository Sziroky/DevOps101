[project]
name = "devops"
version = "0.1.0"
description = "FastAPI Server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.140.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
